#include "widget.h"

wxShylockText::wxShylockText(wxWindow *parent, wxWindowID id, const wxString& text,
                             const wxPoint& pos, const wxSize& size) :
      wxTextCtrl(parent, id, text, pos, size) {
    //nothing to do
}
    
